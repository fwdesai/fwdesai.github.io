# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local Claude Code settings
.claude/settings.local.json

# editor swap files
*.swp
*.swo

# Resume drops in the project root are staging only — the published copy
# lives in public/. This keeps un-vetted versions out of the public repo.
# (Leading slash = root only, so public/*.pdf is still tracked.)
/*.pdf
